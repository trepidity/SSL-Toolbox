{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The desktop webview can open and save files through native dialogs and copy a completed CSR to the system clipboard. Custom commands declared in generate_handler! are not ACL-gated.",
  "windows": ["main"],
  "permissions": [
    "clipboard-manager:allow-write-text",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
