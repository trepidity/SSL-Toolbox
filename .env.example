# Sectigo OAuth credentials (secrets - keep in .env, never in config files)
SCM_CLIENT_ID=<your client id>
SCM_CLIENT_SECRET=<your client secret>

# All other Sectigo configuration belongs in .ssl-toolbox/sectigo.json
# Run `ssl-toolbox init` to generate example config files.
